<% if carrier %>
  <span
    data-toggle="tooltip"
    data-placement="right"
    data-html="true"
    title="
      <b>Name</b>: <%= carrier.company_name.blank? ? "(no name)" : carrier.company_name %><br>
      <b>Ops Name</b>: <%= carrier.poc_name %><br>
      <b>Ops Phone</b>: <%= generate_phone_number("", carrier.primary_phone, carrier.primary_extension_number, carrier.primary_eligible_texting, carrier.primary_phone_type).html_safe %><br/>
      <b>Ops Email</b>: <%= carrier.contact_email && carrier.contact_email.downcase %><br>
      <b>Class</b>: <%= carrier.sales_priority %><br>
      <b>MC Number</b>: <%= carrier.mc_number %><br>
      <b>Reefers Count</b>: <%= carrier.reefers %><br>
      <b>Sate/Province</b>: <%= generate_abv(carrier.state).upcase %><br>
      <b>Last Contact Date</b>: <%= carrier.date_opened.nil? ? "" : carrier.date_opened.strftime('%m/%d/%Y').to_s %><br>
      <b>Last Edit Date</b>: <%= carrier.last_activity_date.nil? ? '' : carrier.last_activity_date.strftime("%m/%d/%Y").to_s %><br>
      <% if params[:controller] != "activities" %>
          <hr>
          <b>Last Activity Name</b>: <%= carrier.campaign_name.blank? ? '(no name)' : carrier.campaign_name %><br>
          <b>Last Activity Type</b>: <%= carrier.activity_type.nil? ? '' : carrier.activity_type %><br>
          <b>Last Activity Status</b>: <%= carrier.activity_status ? 'Open' : 'Closed' %><br>
      <% end %>
    ">
    <%= link_to carrier.company_name.blank? ? "(no name)" : carrier.company_name, carrier, :class => generate_styling_activities2(carrier.last_activity_date) %>
  </span>
<% end %>
