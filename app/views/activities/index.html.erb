<!-- begin page-header -->
<%= render 'layouts/static/header', header_title: 'Activities', sub_header_title: 'Create/Edit/View'%>
<!-- end page-header -->
<!-- begin panel -->
<div class="panel panel-inverse">
  <div class="panel-heading">
    <div class="panel-heading-btn">
    <%= link_to 'New Activity', new_activity_path, :class => 'btn btn-primary btn-xs' %>
    </div>
    <h4 class="panel-title">List of Activities</h4>
  </div>
  <div class="panel-body">
    <table class="table table-striped table-bordered data-table-buttons display nowrap">
    								<thead>
    									<tr class="odd gradeX">
    										<th>Type</th>
    										<th>Client</th>
    										<th>Rep</th>
    										<th>Annual Value</th>
    										<th>Status</th>
                        <th>Date Opened</th>
                        <th>Date Closed</th>
										    <th data-orderable="false">Actions</th>
    									</tr>
    								</thead>
    								<tbody>
                      <% @activities.each do |activity| %>
                        <tr>
                          <td><%= activity.activity_type %></td>
                          <td><%= activity.client.nil? ? "" : (link_to activity.client.company_name, activity.client) %></td>
                          <td><%= activity.rep.nil? ? "" : (link_to "#{activity.rep.first_name} #{activity.rep.last_name}", activity.rep) %></td>
                          <td><%= activity.annual_value %></td>
                          <td><%= activity.status ? 'Open' : 'Closed' %></td>
                          <td><%= convert_date(activity.date_opened) %></td>
                          <td><%= convert_date(activity.date_closed) %></td>
                          <td>
                            <%= link_to '<i class="far fa-eye"></i>'.html_safe, activity_path(:id => activity.id, :previous_controller => params[:controller]), :class => 'btn btn-info btn-xs' %>
                            <%= link_to '<i class="far fa-edit"></i>'.html_safe, edit_activity_path(:id => activity.id, :previous_controller => params[:controller]), :class => 'btn btn-warning btn-xs' %>
                            <%= link_to '<i class="fas fa-trash-alt"></i>'.html_safe, activity_path(:id => activity.id, :previous_controller => params[:controller]), method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger btn-xs'%>
                          </td>
                        </tr>
                      <% end %>
                    </tbody>
                  </table>
  </div>
</div>
<!-- end panel -->
