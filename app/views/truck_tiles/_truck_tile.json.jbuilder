json.extract! truck_tile, :id, :name, :status, :origin, :destination, :details, :carrier_id, :shipper_id, :load_date, :priority, :created_at, :updated_at
json.url truck_tile_url(truck_tile, format: :json)
