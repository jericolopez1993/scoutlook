callProcessingModal();
$("div#sortableKanbanBoards").html("<%= escape_javascript(render 'load_tiles/tiles') %>");
$("div#newTruckForm").find(".modal-body").html("<%= escape_javascript(render 'truck_tiles/form', truck_tile: TruckTile.new, form_id: 'new_truck') %>");
$(".new-truck-origin-multiple").select2({placeholder: "Select origins", allowClear: true, dropdownParent: $("#new_truck")});
$(".new-truck-destination-multiple").select2({placeholder: "Select destinations", allowClear: true, dropdownParent: $("#new_truck")});
$("#newTruckForm").modal('hide');

$(document).ready(function() {
  Tiles.init();
});
