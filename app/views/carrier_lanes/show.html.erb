<%= render 'layouts/static/header', header_title: "#{@carrier_lane.carrier.nil? ? '' : link_to(@carrier_lane.carrier.company_name,  @carrier_lane.carrier)}" %>

<div class="panel panel-inverse">
  <div class="panel-heading">
    <div class="panel-heading-btn">
        <% if current_user.has_role?(:admin) %>
          <%= link_to 'Edit', edit_carrier_lane_path(@carrier_lane), :class => 'btn btn-warning btn-xs' %>
          <%= link_to 'Back', carrier_path(:id => @carrier_lane.carrier_id), :class => 'btn btn-info btn-xs' %>
        <% end %>
    </div>
    <h4 class="panel-title">Information</h4>
  </div>
  <div class="panel-body">
    <div class="row">
      <label class="col-md-3 col-form-label">Priority:</label>
      <div class="col-md-9 lbl-padding-t">
        <%= @carrier_lane.lane_priority %>
      </div>
    </div>
    <div class="row">
      <label class="col-md-3 col-form-label">Origin:</label>
      <div class="col-md-9 lbl-padding-t">
        <%= @carrier_lane.lane_origin %>
      </div>
    </div>
    <div class="row">
      <label class="col-md-3 col-form-label">Destination:</label>
      <div class="col-md-9 lbl-padding-t">
        <%= @carrier_lane.lane_destination %>
      </div>
    </div>
    <div class="row">
      <label class="col-md-3 col-form-label">Trucks Per Week:</label>
      <div class="col-md-9 lbl-padding-t">
        <%= @carrier_lane.truck_per_week %>
      </div>
    </div>
    <div class="row">
      <label class="col-md-3 col-form-label">Preferred Load Days:</label>
      <div class="col-md-9 lbl-padding-t">
        <% @carrier_lane.preferred_load_day_to_array.each do |cr| %>
          <span class="badge badge-inverse"><%= cr %></span>
        <% end %>
      </div>
    </div>
    <div class="row">
      <label class="col-md-3 col-form-label">Notes:</label>
      <div class="col-md-9 lbl-padding-t">
        <%= @carrier_lane.notes.nil? ? "" : @carrier_lane.notes.html_safe %>
      </div>
    </div>

  </div>
</div>
