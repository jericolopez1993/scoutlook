$("div#showLoad").find(".modal-body").html("<%= escape_javascript(render 'load_tiles/show_fields', load_tile: @load_tile) %>");
$("div#showLoad").find(".modal-title").text("<%= @load_tile.origin %> -> <%= @load_tile.destination %>")
$("#showLoad").modal('show');

$(document).ready(function() {
  Tiles.init();
});
